export default function AdminSettingsPage() {
  return (
    <div>
      <h1 style={{
        fontSize: '2.5rem',
        fontWeight: '700',
        color: 'var(--text-primary)',
        marginBottom: '2rem'
      }}>
        Academy Settings
      </h1>
      <p style={{
        color: 'var(--text-secondary)',
        fontSize: '1.1rem'
      }}>
        Manage academy branding, policies, and configurations
      </p>
    </div>
  );
}
