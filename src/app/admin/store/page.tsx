export default function AdminStorePage() {
  return (
    <div>
      <h1 style={{
        fontSize: '2.5rem',
        fontWeight: '700',
        color: 'var(--text-primary)',
        marginBottom: '2rem'
      }}>
        Manage Store
      </h1>
      <p style={{
        color: 'var(--text-secondary)',
        fontSize: '1.1rem'
      }}>
        Add products, manage inventory, and track sales
      </p>
    </div>
  );
}
